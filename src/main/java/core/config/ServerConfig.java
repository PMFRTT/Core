package core.config;

import core.settings.Settings;
import org.bukkit.plugin.Plugin;

public class ServerConfig extends Settings {

    public ServerConfig(String name, Plugin plugin) {
        super(name, plugin);
    }



}
